<% if line_item == @current_item %>
	<tr id="current_item">
	<% else %>
	<tr>
<% end %>
	<td><%= line_item.quantity %>&times;</td>
	<td><%= truncate(line_item.product.title,length: 45) %></td>
	<td class="item_price"><%= format_chf(line_item.total_price) %></td>
</tr>
